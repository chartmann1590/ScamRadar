PLAY STORE DATA SAFETY FORM

The Data Safety form below mirrors what ScamRadar actually does. Use these
exact answers when filling out the Play Console form.

DOES YOUR APP COLLECT OR SHARE ANY USER DATA?
  Yes — anonymous analytics, plus an email address (or Google account
  name/email/photo) only if the user chooses to create an account to set up
  or join a Family pod. Scanning never requires an account.

DOES YOUR APP USE ENCRYPTION IN TRANSIT?
  Yes (Firebase + AdMob use HTTPS).

DO YOU PROVIDE A WAY FOR USERS TO REQUEST DATA DELETION?
  Yes — users can clear scan history from Settings, and uninstalling the
  app removes all on-device data. Users who created an account for Family
  pod can permanently delete that account in-app from Settings → Account →
  Delete account.

DATA TYPES COLLECTED:

  App activity → App interactions (anonymous analytics events)
    Collected: Yes
    Shared:    No
    Optional:  Yes (Firebase Analytics can be disabled at OS level)
    Purpose:   Analytics, App functionality

  Device or other IDs → Optional / required for ad serving (AdMob)
    Collected: Yes (advertising ID)
    Shared:    Yes (with Google/AdMob)
    Optional:  Yes (user can opt-out via Android Settings → Ads)
    Purpose:   Advertising or marketing

  Personal info → Email address, or name/email/photo via Google Sign-In
    Collected: Yes — only if the user creates an account for Family pod
    Shared:    No (used solely for Firebase Authentication sign-in)
    Optional:  Yes (entire app works without an account; only Family pod
               setup/join requires one)
    Purpose:   Account management, App functionality (Family pod)

NOTIFICATION CONTENT (LIVE SHIELD):

  Live Shield uses the Android Notification Listener API to read incoming
  notification text on-device for real-time scam detection. This is
  opt-in (off by default) and revocable at any time in Android Settings →
  Notification access.
    Collected: No (processed transiently on-device only)
    Shared:    No
    Stored:    No (not persisted beyond the on-device alert / local
               history you choose to keep)
    Purpose:   App functionality (real-time scam detection)

DATA TYPES *NOT* COLLECTED:

  - Home address or phone number
  - Messages or message content (scan input)
  - Photos or videos
  - Audio files (voicemail recordings)
  - Files and documents
  - Contacts
  - Calendar
  - Location
  - Financial info
  - Health & fitness
  - Web browsing history
  - SMS messages

KEY GUARANTEE TO SURFACE IN YOUR LISTING

  "Your messages, screenshots, and voicemails are processed entirely on your
   device. They are never uploaded, never logged, and never seen by us."
